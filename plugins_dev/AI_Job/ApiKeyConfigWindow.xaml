<Window x:Class="AI_Job.ApiKeyConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="配置AI API Key" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="请输入您的AI API Key:" 
                   FontSize="14" Margin="0,0,0,10"/>
        
        <PasswordBox x:Name="ApiKeyTextBox" Grid.Row="1" 
                     FontSize="12" Padding="5" Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="保存" Width="80" Height="30" Margin="0,0,10,0" 
                    Click="SaveButton_Click"/>
            <Button Content="取消" Width="80" Height="30" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
